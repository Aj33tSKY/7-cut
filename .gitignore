# Secrets
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
ENV/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# webapp — job workspaces + sqlite db are runtime state, not source
webapp/data/

# webapp — filled-in copy of the .example template, contains a real OAuth
# client secret once edited locally
webapp/get_drive_refresh_token.sh

# Node (rarely needed; nothing in this repo requires it by default)
node_modules/
package-lock.json
yarn.lock
.npm/

# Manim render artifacts (when running skills/manim-video scripts locally)
media/
*.log

# Session artifacts if someone invokes the skill inside the project dir
# (normally outputs go to <videos_dir>/edit/ — keep these as a safety net)
edit/
transcripts/
animations/
clips_graded/
clips_preview/
downloads/
verify/
preview.mp4
final.mp4
base.mp4
base_preview.mp4
master.srt
takes_packed.md
edl.json
project.md
